{% extends 'admin/base.html.twig' %}

{% block title "Modification du type de boite " %}

{% block body %}
<main id='admin_gearbox_edit' class='admin_crud_edit'>
    <div class="header">
        <h2>Modification du type de boite</h2>
    </div>
    <div class="form">
    {{ form_start(form) }}
    <div class="grid-two">
        <div class="input-text">
            <label>Id :</label>
            {{ form_widget(form.id)}}
        </div>
        <div class="input-text">
            <label>Nom :</label>
            {{ form_widget(form.gearbox)}}
        </div>
    </div>
        <div class="submit">
            {{ form_widget(form.submit) }}
        </div>
    {{ form_end(form) }}
    </div>
</main>

{% endblock %}