{% extends "admin/base.html.twig" %}

{% block title 'Ajouter des types de boite' %}

{% block body %}
<main id='admin_gearbox_add' class='admin_crud_create'>
    <div class="header">
        <h2>Ajouter des types de boite</h2>
    </div>
    <div class="form">
    {{ form_start(form) }}
        <div class="grid-two">
            <div class="input-text">
                <label>Id :</label>
                {{ form_widget(form.id)}}
            </div>
            <div class="input-text">
                <label>Nom :</label>
                {{ form_widget(form.gearbox)}}
            </div>
        </div>
        <div class="submit">
            {{ form_widget(form.submit) }}
        </div>
    {{ form_end(form) }}
    </div>
</main>

{% endblock %}